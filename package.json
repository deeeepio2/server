{
	"name": "server",
	"version": "0.0.1",
	"devDependencies": {
		"@types/bun": "^1.1.14",
		"typescript": "^5.7.2"
	},
	"scripts": {
		"start": "bun run index.ts",
		"git-update": "git pull && git submodule update --remote --merge"
	},
	"type": "module",
	"dependencies": {
		"elysia": "^1.2.2"
	}
}
